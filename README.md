# Simple Go Web Service

This project is a lightweight backend service built using Go.

It provides a basic HTTP endpoint that returns a JSON response.

## What it does

When you open the main route /, the server responds with a message in JSON format.

Example response:

```json
{
  "message": "API is working"
}
